// Adjust this to match the port your ASP.NET Core API runs on locally.
// Must include the /api prefix — controllers are routed under api/[controller].
// Find the port in Properties/launchSettings.json, or the console output when you `dotnet run`.
window.API_BASE_URL = "https://localhost:7294/api";
